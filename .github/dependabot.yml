version: 2
updates:
  # Keeps the SHA-pinned actions in the workflows up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Gives "ci: bump ..." PR titles, which the PR title check accepts.
    commit-message:
      prefix: ci
